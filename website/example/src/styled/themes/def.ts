/**
 *  default 主题变量
 */

import { DefaultTheme } from 'styled-components'

import { DeepPartial } from '@core/utils/types'

const defaultTheme: DeepPartial<DefaultTheme> = {
  colors: {
    layoutHeaderBg: '#fff',
  },
}

export default defaultTheme
