import React from 'react'

// import { useEditor } from '@craftjs/core'
// import { Text, Button as UiButton } from '@fluentui/react'

// import { Button } from '../user/button'

export default () => {
  // const { connectors } = useEditor()

  return (
    <div>
      {/* <Text>Drag to add1</Text> */}
      <div>
        {/* <UiButton
          ref={(ref: any) => connectors.create(ref, <Button text="Click me" size="small" />)}
        >
          Button
        </UiButton> */}
      </div>
    </div>
  )
}
